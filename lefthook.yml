pre-commit:
  parallel: true
  commands:
    format:
      run: deno fmt --check {staged_files}
    lint:
      run: deno lint {staged_files}

pre-push:
  commands:
    test:
      run: deno task test
