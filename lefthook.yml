pre-commit:
  parallel: true
  commands:
    format:
      run: deno fmt --check {staged_files}
    lint:
      run: deno lint {staged_files}

pre-push:
  parallel: true
  commands:
    check:
      run: deno task check
    test:
      run: deno task test
